[build]
  command = "npm install && npm run build"
  functions = "functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["puppeteer", "express", "import-fresh"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200